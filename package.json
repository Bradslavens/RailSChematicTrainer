{
  "name": "rail-schematic-trainer",
  "version": "0.0.0",
  "private": true,
  "description": "Gamified web app to learn light-rail (San Diego MTS) schematics for a job qualification test.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspace server & npm run dev --workspace web",
    "build": "npm run build --workspaces --if-present"
  },
  "license": "MIT"
}
